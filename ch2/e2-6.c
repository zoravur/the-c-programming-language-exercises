




unsigned setbits(unsigned x, unsigned p, unsigned n, unsigned y) {
    // 010010011
    // 000000100
    // (x, 3, 3, y)
    // 010100011
    unsigned allones = ~0;
    unsigned bits_to_assign = y & ~(allones << n);
    unsigned 
    
    



}
